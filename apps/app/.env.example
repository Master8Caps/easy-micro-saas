# ===========================================
# App Environment Variables
# ===========================================
# Copy this file to .env.local for local development.
# Never commit .env.local or any .env file.

# Public (exposed to browser)
NEXT_PUBLIC_APP_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase — URL and anon key use NEXT_PUBLIC_ prefix (needed by browser client).
# The anon key is safe to expose — RLS enforces access control.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key    # Server-only. Never expose to client.

# AI / LLM (server-only)
ANTHROPIC_API_KEY=sk-ant-...                       # Server-only. Primary LLM for generation.
OPENAI_API_KEY=sk-...                              # Server-only. Used for embeddings only.

# Stripe (server-only)
STRIPE_SECRET_KEY=sk_test_...                      # Server-only. Billing integration.
STRIPE_WEBHOOK_SECRET=whsec_...                    # Server-only. Webhook signature verification.
